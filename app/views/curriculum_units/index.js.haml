$('.allocations').append('#{escape_javascript(render partial: 'curriculum_units', locals: {curriculum_units: @curriculum_units, type: @type})}');
$('a.load-more-allocations').attr('href', '#{curriculum_units_path(page: @curriculum_units.next_page, type_id: @type.id)}');
$(".tb_list").trigger("update");

- unless @curriculum_units.next_page
  $('a.load-more-allocations').remove();