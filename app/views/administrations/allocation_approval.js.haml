$('.allocations').append('#{escape_javascript(render partial: "pending_allocations", locals: { allocations: @allocations })}');
$('a.load-more-allocations').attr('href', '#{search_allocation_administrations_path page: @allocations.next_page, type: @type_search, value: @text_search}');
$(".tb_list").trigger("update");

- unless @allocations.next_page
  $('a.load-more-allocations').remove();
