.block_wrapper
  .block_title
    %h2
      %i.icon-suitcase
      = t(:group)
      = @group.code

  / Lista dos alunos com notas

  %div.tb_list_students
    %table.tb_list#list_of_students{:cellspacing => "0", :border => "0", :summary => "lista de estudantes"}
      %thead
        %tr.lines
          %th{:align => "left", :id => "texte2"}= t(:student)
          %th{:id=>"public_files"}= t(:public_files)
          %th.participation= t(:participation)
          %th.access_history= t(:access_to_the_course)
      %tbody
        - @students.each_with_index do |student, idx|
          %tr.lines
            %td.td_student{:align => "left", 'data-tooltip' => t(student.name)}= link_to student.name, student_scores_path(student.id)
            %td{:align => "center"}= @scores["student_count_public_files"][idx]
            %td{:align => "center"}= "-"
            %td{:align => "center"}= @scores["student_count_access"][idx]


  %div.tb_list_jobs
    %table.tb_list#list_of_students{:cellspacing => "0", :border => "0", :summary => "lista de estudantes"}
      %thead
        %tr.lines
          %th{:colspan => "#{@assignments.length}", :style => "width: 20%;", :id => "tests"}= t(:private_area)
        %tr#activities
          - unless @assignments.empty?
            - @assignments.each_with_index do |assignment, idx|
              %th{'data-tooltip' => t(assignment.name)}= "#{idx + 1}"
          - else
            %th{:style => "padding-top: 24px;"}
        - @students.each_with_index do |student, idx|
          %tr.lines
            - @assignments.each_with_index do |assignment, idx2|
              - grade = @scores["students_grades"][idx][idx2]
              - grade_link = t("assignment_#{grade}", :default => grade)
              %td{:align => "center", :class => "assignment_#{grade}"}
                - if grade == "without_group"
                  = link_to t(:assignment_wg), information_assignment_path(assignment.id)
                - else
                  = link_to grade_link, student_assignment_path(assignment.id, :student_id => student.id, :group_id => @scores["students_groups"][idx][idx2]) 
            - if @assignments.empty?
              %td{:align => "center"}= "-"

/ Legenda das notas
#scores_notes.legend_score_footer
  %div.assignment_as
    = t(:assignment_as)
    = t(:assignment_without_correction)
  %div.assignment_an
    = t(:assignment_an)
    = t(:assignment_not_sent)
  %div.assignment_without_group
    = t(:assignment_wg)
    = t(:student_without_group)

/ Paginacao
/ .legend#scores_pagination{:style => "text-align: center;"}
/   %span=raw render_pagination_bar(@students.size) unless @students.empty?

/ Legenda dos trabalhos
/ - unless @assignments.empty?
/   .legend
/     %ul
/       %li
/         %h1= t(:legend_assignments)
/       - @assignments.each_with_index do |assignment, idx|
/         %li
/           = "#{idx + 1}º - "
/           = assignment.name