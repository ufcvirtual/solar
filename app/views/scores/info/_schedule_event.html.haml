%section.schedule_event
  .block_title
    %h2= t(:schedule_event, :scope => [:scores, :info])
    #tabs.tab_event
      %ul
        %li= link_to t(:evaluative, scope: [:scores, :info]), '#', class: 'evaluative active', onclick: 'change_tab_tool(this); return false;', :'data-div' => '#evaluative'
        %li= link_to t(:frequency, scope: [:scores, :info]), '#', class: 'frequency', onclick: 'change_tab_tool(this); return false;', :'data-div' => "#frequency"
        %li= link_to t(:not_evaluative, scope: [:scores, :info]), '#', class: 'notevaluative', onclick: 'change_tab_tool(this); return false;', :'data-div' => "#notevaluative"
  .block_content
    .tools.show#evaluative
      .block_content
        - unless @schedules_event_evaluative.blank?
          %table.tb_list
            %thead
              %tr.lines
                %th{style: "width: 6%;"}= t(:schedule_event, scope: [:scores, :info])
                %th.center.date{style: "width: 6%;"}= t(:date_range, scope: [:scores, :info])
                %th.center{style: "width: 4%;"}= t(:situation, scope: [:scores, :info])
                %th.center{style: "width: 2%;"}= t(:grade, scope: [:scores, :info])
                - if @can_evaluate_ev
                  %th.center.no_sort{style: "width: 2%;"}
            %tbody
              - @schedules_event_evaluative.each do |schedule_event|
                %tr.lines
                  %td{align: "left"}
                    .title= schedule_event.name
                    .description
                      .minimun= sanitize(truncate schedule_event.description, length: 100, omission: "#{content_tag(:i, nil, class: 'expand icon-ellipsis', :"data-tooltip" => t(".expand_description"))}")
                      .complete.invisible
                        = schedule_event.description
                        = content_tag(:i, nil, class: "compress icon-arrow-up-triangle", :"data-tooltip" => t(".compress_description"))
                    .equivalent
                      - unless schedule_event.eq_name.blank?
                        %a{ href: '#', :"data-dropdown" => "#dropdown_eq_1_#{schedule_event.id}" }
                          %i.icon-flow-parallel
                          = t('scores.info.equivalency')
                          %i.icon-arrow-down-triangle
                        .dropdown.dropdown-tip{id: "dropdown_eq_1_#{schedule_event.id}"}
                          %ul{ class: 'dropdown-menu' }
                            = raw t('scores.info.equivalent', name: schedule_event.eq_name)
                      - else
                        %a{ href: '#', :"data-dropdown" => "#dropdown_eq_1_#{schedule_event.id}", :'data-url' => equalities_tools_path(tool_type: schedule_event.academic_tool_type, ac_id: schedule_event.id), onclick: 'open_dropdown(this);' }
                          %i.icon-flow-parallel
                          =t('scores.info.equalities')
                          %i.icon-arrow-down-triangle
                        .dropdown.dropdown-tip{id: "dropdown_eq_1_#{schedule_event.id}"}
                          %ul{ class: 'dropdown-menu' }
                  %td.center
                    %div= [l(schedule_event.start_date.to_date, format: :normal), l(schedule_event.end_date.to_date, format: :normal)].join(" - ")
                    - if !schedule_event.start_hour.blank?
                      %div= [(schedule_event.start_hour), (schedule_event.end_hour)].join(' - ')
                  %td.center{class: "assignment_#{schedule_event.situation}"}= t("scores.situation.#{schedule_event.situation}")
                  %td.center
                    = schedule_event.grade
                  - if @can_evaluate_ev 
                    %td.center
                      = link_to content_tag(:i, nil, class: 'icon-write'), evaluate_user_schedule_event_path(schedule_event.academic_tool_id, user_id: @user.id), class: 'btn to_evaluate_schedule_event', :'data-tooltip'=> t(:to_evaluate, scope: [:posts, :post])   
        - else
          .block_content.block_content_text= t(:itens_not_found)

    .tools#frequency
      .block_content
        - unless @schedules_event_frequency.blank?
          %table.tb_list
            %thead
              %tr.lines
                %th{style: "width: 6%;"}= t(:schedule_event, scope: [:scores, :info])
                %th.center.date{style: "width: 6%;"}= t(:date_range, scope: [:scores, :info])
                %th.center{style: "width: 4%;"}= t(:situation, scope: [:scores, :info])
                %th.center{style: "width: 2%;"}= t(:frequency, scope: [:scores, :info])
                - if @can_evaluate_ev
                  %th.center.no_sort{style: "width: 2%;"}
            %tbody
              - @schedules_event_frequency.each do |schedule_event|
                %tr.lines
                  %td{align: "left"}
                    .title= schedule_event.name
                    .description
                      .minimun= sanitize(truncate schedule_event.description, length: 100, omission: "#{content_tag(:i, nil, class: 'expand icon-ellipsis', :"data-tooltip" => t(".expand_description"))}")
                      .complete.invisible
                        = schedule_event.description
                        = content_tag(:i, nil, class: "compress icon-arrow-up-triangle", :"data-tooltip" => t(".compress_description"))
                    .equivalent
                      - unless schedule_event.eq_name.blank?
                        %a{ href: '#', :"data-dropdown" => "#dropdown_eq_2_#{schedule_event.id}" }
                          %i.icon-flow-parallel
                          = t('scores.info.equivalency')
                          %i.icon-arrow-down-triangle
                        .dropdown.dropdown-tip{id: "dropdown_eq_2_#{schedule_event.id}"}
                          %ul{ class: 'dropdown-menu' }
                            = raw t('scores.info.equivalent', name: schedule_event.eq_name)
                      - else
                        %a{ href: '#', :"data-dropdown" => "#dropdown_eq_2_#{schedule_event.id}", :'data-url' => equalities_tools_path(tool_type: schedule_event.academic_tool_type, ac_id: schedule_event.id), onclick: 'open_dropdown(this);' }
                          %i.icon-flow-parallel
                          =t('scores.info.equalities')
                          %i.icon-arrow-down-triangle
                        .dropdown.dropdown-tip{id: "dropdown_eq_2_#{schedule_event.id}"}
                          %ul{ class: 'dropdown-menu' }
                  %td.center
                    %div= [l(schedule_event.start_date.to_date, format: :normal), l(schedule_event.end_date.to_date, format: :normal)].join(" - ")
                    - if !schedule_event.start_hour.blank?
                      %div= [(schedule_event.start_hour), (schedule_event.end_hour)].join(' - ')
                  %td.center{class: "assignment_#{schedule_event.situation}"}= t("scores.situation.#{schedule_event.situation}")
                  %td.center
                    = schedule_event.working_hours
                  - if @can_evaluate_ev
                    %td.center
                      = link_to content_tag(:i, nil, class: 'icon-write'), evaluate_user_schedule_event_path(schedule_event.academic_tool_id, user_id: @user.id), class: 'btn to_evaluate_schedule_event', :'data-tooltip'=> t(:to_evaluate, scope: [:posts, :post])   
        - else
          .block_content.block_content_text= t(:itens_not_found)
  
    .tools#notevaluative 
      .block_content
        - unless @schedules_event_not_evaluative.blank?
          %table.tb_list
            %thead
              %tr.lines
                %th{style: "width: 6%;"}= t(:schedule_event, scope: [:scores, :info])
                %th.center.date{style: "width: 6%;"}= t(:date_range, scope: [:scores, :info])
                %th.center{style: "width: 4%;"}= t(:situation, scope: [:scores, :info])

            %tbody
              - @schedules_event_not_evaluative.each do |schedule_event|
                %tr.lines
                  %td{align: "left"}
                    .title= schedule_event.name
                    .description
                      .minimun= sanitize(truncate schedule_event.description, length: 100, omission: "#{content_tag(:i, nil, class: 'expand icon-ellipsis', :"data-tooltip" => t(".expand_description"))}")
                      .complete.invisible
                        = schedule_event.description
                        = content_tag(:i, nil, class: "compress icon-arrow-up-triangle", :"data-tooltip" => t(".compress_description"))
                    .equivalent
                      - unless schedule_event.eq_name.blank?
                        %a{ href: '#', :"data-dropdown" => "#dropdown_eq_3_#{schedule_event.id}" }
                          %i.icon-flow-parallel
                          = t('scores.info.equivalency')
                          %i.icon-arrow-down-triangle
                        .dropdown.dropdown-tip{id: "dropdown_eq_3_#{schedule_event.id}"}
                          %ul{ class: 'dropdown-menu' }
                            = raw t('scores.info.equivalent', name: schedule_event.eq_name)
                      - else
                        %a{ href: '#', :"data-dropdown" => "#dropdown_eq_3_#{schedule_event.id}", :'data-url' => equalities_tools_path(tool_type: schedule_event.academic_tool_type, ac_id: schedule_event.id), onclick: 'open_dropdown(this);' }
                          %i.icon-flow-parallel
                          =t('scores.info.equalities')
                          %i.icon-arrow-down-triangle
                        .dropdown.dropdown-tip{id: "dropdown_eq_3_#{schedule_event.id}"}
                          %ul{ class: 'dropdown-menu' }
                  %td.center
                    %div= [l(schedule_event.start_date.to_date, format: :normal), l(schedule_event.end_date.to_date, format: :normal)].join(" - ")
                    - if !schedule_event.start_hour.blank?
                      %div= [(schedule_event.start_hour), (schedule_event.end_hour)].join(' - ')
                  %td.center{class: "assignment_#{schedule_event.situation}"}= t("scores.situation.#{schedule_event.situation}")
        - else
          .block_content.block_content_text= t(:itens_not_found)

= javascript_include_tag 'tablesorter', 'tooltip', 'scores'

:javascript

  $(function(){
   $(".to_evaluate_schedule_event").call_fancybox({
      minWidth: '50%',
      minHeight: '50%',
      height: 'auto',
      closeBtn : true
    });
  }); 