- if @access.any?
  %thead
    %th#access_date.center= t(".date")
    %th#access_time.center= t(".time")
  %tbody
    - @access.each do |access|
      %tr.lines
        %td.center{headers: 'access_date'}= l(access.created_at.to_datetime, format: :normal)
        %td.center{headers: 'access_time'}= l(access.created_at.to_datetime, format: :clock_time)
- else
  %tbody
    %tr.block_content.block_content_text.text_none
      %th= t(".no_access")