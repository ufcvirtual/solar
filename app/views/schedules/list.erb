<!--
<div>
  <span class="page_title"><%= t(:agenda_title)%></span>
  <span class="page_title"><%=@curriculum_unit.name%></span>
</div>

-->

<div class="agenda_content">
  <% if !(@schedule.nil? || @schedule.empty?) %>
    <% @schedule.each do |schedule| %>

      <div class="agenda_block">
        <div class="agenda_left">
          <div class="agenda_left_title">
            <div class="agenda_left_title_img <%= schedule['schedule_type'] %>"></div>
            <div class="agenda_left_title_text"><%=t :"#{schedule['schedule_type']}" %></div>
          </div>
          <div class="agenda_left_content">
            <div class="agenda_left_date">
              <% start_date = schedule['start_date'].nil? ? nil : schedule['start_date'].to_date %>
              <% end_date = schedule['end_date'].nil? ? nil : schedule['end_date'].to_date %>
              <%=raw display_date_format(start_date, end_date) %>
            </div>
          </div>
        </div>
        <div class="agenda_right">
          <span class="title"><%= schedule['name'] %></span>
          <div class="content">
            <p><%= schedule['description'] %></p>
          </div>
        </div>
      </div>

    <% end %>
  <% else %>
    <%= t(:agenda_null) %>
  <% end %>
</div>
