%td= check_box_tag( @allocation.id, @allocation.id, false, {:"status-value" => @allocation.status, class: "cbx_value"} )
%td= @allocation.user.name
%td= @allocation.user.email
%td= @allocation.group.code_semester
%td= @allocation.group.curriculum_unit.try(:name)
%td= name_of(@allocation.status)
%td= button_tag t('allocations.index.manage'), type: 'button', onclick: "manage_enrollment(this)", class: "btn btn_default btn_manage_enrollment", "show-link" => allocation_path(@allocation), :"edit-link" => edit_allocation_path(@allocation)

:javascript
  eval("#{show_flash}");
