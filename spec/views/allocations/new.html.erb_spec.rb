require 'spec_helper'

describe "allocations/new" do
  # before(:each) do
  #   assign(:allocation, stub_model(Allocation).as_new_record)
  # end

  # it "renders new allocation form" do
  #   render

  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form", :action => allocations_path, :method => "post" do
  #   end
  # end
end
